[project]
name = "opendp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "beiyu", email = "qifenglu13@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
opendp = "opendp:main"

[build-system]
requires = ["uv_build>=0.11.19,<0.12.0"]
build-backend = "uv_build"
